[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "coldwatch"
version = "0.0.0"
description = "A tripwire for cold storage"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    # The only runtime dependency, and the only one the design has room for: the ZMQ client
    # for bitcoind's publisher. Everything else here is stdlib on purpose -- see SECURITY.md.
    "pyzmq>=25",
]

[project.urls]
Homepage = "https://github.com/Wired4ncer/coldwatch"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
coldwatch = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
src = ["src", "tests", "tools"]
