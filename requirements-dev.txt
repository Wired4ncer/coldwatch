# Development and test dependencies — on top of the package itself.
# Install the package too (`pip install -e .`): the tests import coldwatch, and pyzmq,
# coincurve and websocket-client are declared there as RUNTIME dependencies, not tooling ones.

pyzmq>=25.0            # also a runtime dependency — see pyproject.toml and SECURITY.md.
                       # Used by the live subscriber, and by tools/replay_zmq.py.
coincurve>=20.0        # also a runtime dependency — Nostr channel signing/ECDH (issue #3).
websocket-client>=1.7  # also a runtime dependency — Nostr channel relay publish (issue #3).

# tools/fake_rpc.py and tools/make_fixtures.py are stdlib-only, by design —
# a contributor can run the RPC fixture with nothing installed at all.

# Test + lint
pytest>=8.0
ruff>=0.5
